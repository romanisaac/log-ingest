[package]
name = "storage"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { workspace = true }
object_store = { version = "0.11", features = ["aws"] }

[dev-dependencies]
tokio = { workspace = true }
tempfile = "3"
bytes = "1"
